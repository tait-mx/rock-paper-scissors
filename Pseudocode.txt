Pseudocode

1 Create funtion humanChoice() that takes an input value from promt and returns value
2 Create function computerChoice() that randomly chooses "rock", "paper" or "scissors" 
    returns result
3 Create an alert in function computerChoice() showing computers choice
4 Create comparison() function that calls humanChoice() and computerChoice() funcs,
5 Compare them and return who wins.
6 Create alert in comparison() that shows round result and store winner value
    in var roundWinner
7 Create round() fuction to keep track of who wins each round in var roundCount
8 Create gameCount() function that finishes the game and restarts it if roundCount == 5