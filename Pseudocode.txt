Pseudocode

1 Create function humanChoiceGetting() that takes an input value from promt and stores it in
var humanChoice
2 Create function computerChoiceGetting() that randomly chooses "rock", "paper" or "scissors" 
    and stores the result in var computerChoice
4 Create comparison() function that compares computerChoice and humanChoice vars, 
    stores who wins and alerts the result
6 Within the comparison() function store the winner value in var roundWinner
7 Create round() fuction to keep track of who wins each round in var roundCount
8 Create gameCount() function that finishes the game and restarts it if roundCount == 5